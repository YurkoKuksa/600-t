export const Icon = ({ name, size }) => {
  switch (name) {
    case "gh":
      return <div size={size}>Icon</div>;

    case "arrow-up":
      return (
        <>
          <p size={size}>hjk</p>
        </>
      );

    default:
      return <>hj</>;
  }
};
